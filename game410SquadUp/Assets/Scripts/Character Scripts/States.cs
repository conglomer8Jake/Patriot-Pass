using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class States : MonoBehaviour
{
    // Start is called before the first frame update

     public enum PlayerState {field, Puck, Menu};

}

